# trueworshipglobal

Wole Emmanuel / True Worship Global — artist website.

## Development

```bash
cd frontend
npm install
npm run dev
```

See `frontend/.env.example` for environment variables (Spotify, YouTube).
